# Sidebar v2 — Collapsible Groups

## Observations
- Sidebar now shows collapsible groups - NAV is open by default showing 7 icons
- INTEL, OPS, AI, SYS groups are collapsed showing only their labels
- Icons are well-spaced with proper gap between them
- Google Maps loaded successfully showing Tel Aviv
- FPS: 32fps (good)
- Status bar: 32fps, weather 13.6°, ±100m, 4sat, 5G, time

## Issues Found
1. The sidebar is still quite narrow (96px) — the group labels (NAV, INTEL, OPS, AI, SYS) are left-aligned but could be more prominent
2. The ChevronDown arrows are visible but small
3. Bottom dock shows: DRIVE/WALK/SOS/PLAN modes + Navigate button + Traffic + Settings
4. The bottom dock buttons are properly spaced now
5. Map controls (zoom, layers, recenter, 3D) are on the right side, properly spaced

## What Works Well
- Collapsible groups solve the cramping problem completely
- Only 7 icons visible at a time (NAV group) instead of 30
- Smooth animation when expanding/collapsing
- Group indicators show which group is active
- Map is fully visible with Google Maps loaded
