version: "3.9"

services:
  gane-nav:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gane-nav
    ports:
      - "3000:3000"
    environment:
      - RUST_LOG=info
      - GANE_PORT=3000
    restart: unless-stopped
    healthcheck:
      test: ["/usr/local/bin/gane-nav", "--status"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
