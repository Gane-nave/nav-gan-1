[package]
name = "gane-subsystem-sync"
version = "0.1.0"
edition = "2021"
description = "Consistency arbitration between navigation subsystems."

[dependencies]
serde = { version = "1", features = ["derive"] }
