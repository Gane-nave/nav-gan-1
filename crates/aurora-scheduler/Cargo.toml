[package]
name = "aurora-scheduler"
version = "0.1.0"
edition = "2021"
description = "Task scheduling and job queue — cron-like scheduling, job priorities, execution tracking, retry policies"

[dependencies]
aurora-core = { path = "../aurora-core" }
