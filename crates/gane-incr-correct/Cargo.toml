[package]
name = "gane-incr-correct"
version = "0.1.0"
edition = "2021"
description = "Incremental position correction without hard resets."

[dependencies]
serde = { version = "1", features = ["derive"] }
