# Prometheus configuration for AURORA NAV local development
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "aurora-nav"
    static_configs:
      - targets: ["aurora-nav:3000"]
    metrics_path: /metrics
    scrape_interval: 10s
