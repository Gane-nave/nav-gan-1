[package]
name = "aurora-determinism"
version = "0.1.0"
edition = "2021"
description = "Deterministic navigation for reproducible output."

[dependencies]
serde = { version = "1", features = ["derive"] }
