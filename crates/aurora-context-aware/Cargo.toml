[package]
name = "aurora-context-aware"
version = "0.1.0"
edition = "2021"
description = "Context-aware navigation adapting to environment."

[dependencies]
serde = { version = "1", features = ["derive"] }
