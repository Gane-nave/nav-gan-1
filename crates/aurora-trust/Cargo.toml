[package]
name = "aurora-trust"
version.workspace = true
edition.workspace = true
description = "Trust system — reputation scoring, multi-source validation, sybil/collusion detection"

[dependencies]
aurora-core = { path = "../aurora-core" }
serde = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }
tracing = { workspace = true }
