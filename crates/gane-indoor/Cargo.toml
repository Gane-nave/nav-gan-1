[package]
name = "gane-indoor"
version = "0.1.0"
edition = "2021"
description = "Indoor positioning system using WiFi, BLE beacons, and magnetic fingerprinting"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
