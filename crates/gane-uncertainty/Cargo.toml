[package]
name = "gane-uncertainty"
version = "0.1.0"
edition = "2021"
description = "Uncertainty modeling with covariance propagation."

[dependencies]
serde = { version = "1", features = ["derive"] }
