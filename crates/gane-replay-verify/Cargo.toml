[package]
name = "gane-replay-verify"
version.workspace = true
edition.workspace = true
description = "Golden-trace replay harness: sensor logs -> canonical ESKF -> reproducible state (native/WASM equivalence proof)"

[dependencies]
gane-fusion = { path = "../gane-fusion" }
nalgebra = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
