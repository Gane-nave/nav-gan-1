[package]
name = "aurora-prefetch"
version.workspace = true
edition.workspace = true
description = "Route and map data prefetching engine"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
