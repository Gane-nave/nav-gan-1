[package]
name = "aurora-numerical"
version = "0.1.0"
edition = "2021"
description = "Numerical stability for navigation computations."

[dependencies]
serde = { version = "1", features = ["derive"] }
