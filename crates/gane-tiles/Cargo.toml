[package]
name = "gane-tiles"
version.workspace = true
edition.workspace = true
description = "Vector tile loading engine for map rendering"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
