[package]
name = "aurora-route-quality"
version = "0.1.0"
edition = "2021"
description = "Route confidence and quality scoring engine."

[dependencies]
serde = { version = "1", features = ["derive"] }
