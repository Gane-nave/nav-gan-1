[package]
name = "gane-device-caps"
version.workspace = true
edition.workspace = true
description = "Device capability matrix — GNSS sensors dual-band"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
