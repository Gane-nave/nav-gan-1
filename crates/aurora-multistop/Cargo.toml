[package]
name = "aurora-multistop"
version.workspace = true
edition.workspace = true
description = "Multi-destination route planning"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
