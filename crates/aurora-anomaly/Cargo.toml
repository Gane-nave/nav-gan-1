[package]
name = "aurora-anomaly"
version.workspace = true
edition.workspace = true
description = "GPS and speed anomaly detection"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
