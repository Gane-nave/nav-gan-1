[package]
name = "aurora-trie"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Prefix trie for string key lookups, autocomplete, and prefix matching"

[dependencies]
