[package]
name = "gane-smooth-nav"
version.workspace = true
edition.workspace = true
description = "Smooth navigation — weighted blending, no jumps, stable heading"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
