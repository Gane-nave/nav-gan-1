# סקירת מערכת מקצה לקצה — G.A.N.E / AURORA NAV / GMIN

**תאריך:** 2026-07-12 · **היקף:** שש ריפוזיטוריות, כל קובץ, כל שכבה, כל חיבור
**שיטה:** קריאת מסמכי החזון (PDF + Markdown), ניתוח 2,452 crates, קומפילציה והרצה חיה של המערכת, חילוץ וניתוח כל ארכיוני ה-ZIP, שחזור היסטוריית 294 קומיטים, מיפוי גרף התלויות המלא.

---

## 1. החזון והרציונל (כפי שעולה מהמסמכים)

מתוך `G.A.N (2).pdf`, `USER_REQUIREMENTS_COMPREHENSIVE.md`, ו-README של gane-project:

- **המוצר:** מערכת ניווט מתקדמת לאזור ישראל/פלסטין בעיצוב "קוונטי" עתידני, עברית תחילה (RTL מלא), עם ערבית ואנגלית.
- **קהל היעד:** נהגים מקצועיים, מנהלי ציים, שירותי חירום, פעילות ביטחונית/צבאית.
- **הרציונל ההנדסי המבדל:** *"כל אפליקציית ניווט אחרת סומכת על GNSS. G.A.N.E מתייחסת אליו כאות אחד מני רבים, מאמתת אותו ברציפות מול המציאות, ומסרבת לפעול כשהיא לא סומכת על עצמה"* — כלומר: אמינות (Integrity) לפני נוחות. RAIM, זיהוי שיבוש/זיוף (jamming/spoofing), ניווט אינרציאלי במנהרות, פעולה מלאה במצב לא-מקוון.
- **צורת ההפצה:** PWA → APK דרך TWA (מזהה אפליקציה `com.gane.nav`), דפדפן/מובייל/טאבלט/דסקטופ.
- **יכולות-על נדרשות:** פרופילי רכב (11 סוגים כולל משאיות כבדות עם מגבלות גובה/משקל/חומ"ס), ניתוב מרובה-אמצעים, V2X, ניווט AR, תאום דיגיטלי, מרכז שליטה (C4ISR/EOC), שיתוף פעולה בזמן אמת, תשלומים (Stripe, ₪), טלמטריה ואנליטיקה.

---

## 2. מה קיים בפועל — מפת המערכת האמיתית

המערכת היא **אקוסיסטם של ארבע יחידות נפרדות שאינן מחוברות זו לזו**:

### 2.1 ליבת Rust — AURORA NAV / GMIN (זהה ביט-לביט ב-4 ריפוזיטוריות)

**מאומת בהרצה חיה:** הבינארי `aurora-nav` נבנה, רץ, מגיש 16 endpoints (בהם `/health`, `/position`, `/integrity`, `/metrics`, `/openapi.json`) ודשבורד Leaflet מוטמע. תת-המערכות מדווחות תקינות.

| מדד | מספר מוצהר | מספר אמיתי |
|---|---|---|
| Crates | 2,452 (בקומיטים: "2,606") | **~198 אמיתיים** (8%) |
| שורות קוד Rust | 373,674 | **~150K בקוד אמיתי** (40%) |
| טסטים | 18,497 (בקומיטים: "19,509") | **~4,507 משמעותיים** (24%) |
| מחוברים לגרף התלויות | — | **רק 100 crates**; 2,352 יתומים |

**הליבה האמיתית (מאומתת קוד-שורה-שורה):**
- `aurora-gnss` — פותר PVT אמיתי ב-Weighted Least Squares (ECEF, pseudoranges, שקלול CN0, חישוב PDOP/HDOP/VDOP).
- `aurora-fusion` — מסנן קלמן מורחב (EKF) אמיתי בן 9 מצבים עם יעקוביאנים, רעש תהליך, ואכיפת סימטריה של הקווריאנס.
- `aurora-integrity` — זיהוי jamming/spoofing/multipath על בסיס היסטוריית CN0.
- `aurora-routing` — Dijkstra עם binary heap ופונקציות עלות מתחלפות (מרחק/זמן/ללא-אגרות/הימנעות ממנהרות).
- `aurora-map`, `aurora-traffic` (הגדול בתחומו — 1,426 שורות), `aurora-v2x`, `aurora-indoor` (טרילטרציית BLE), `aurora-ar-nav` (הטלת פינהול).
- `aurora-api` (Axum, 16 endpoints) + `aurora-web` (דשבורד Leaflet מוטמע 41KB) + `aurora-app` (הבינארי; תלוי ב-83 crates — נקודת האינטגרציה).
- 88 קבצי טסטים אדברסריאליים + חבילות e2e אמיתיות.
- Dockerfile multi-stage אמיתי + docker-compose עם healthcheck.

**הניפוח:** 2,254 crates (92%) הם תבנית זהה שנוצרה אוטומטית: struct עם 5 דגלים בוליאניים, `health_score()` שמחזיר 5.0 או 100.0, ו-6 טסטים שבודקים ערכי `true` קבועים. שמות מבטיחים כמו `aurora-neural-net`, `aurora-blockchain-log`, `aurora-msg-kafka`, `aurora-slam-engine` — **אינם מכילים דבר מהפונקציונליות שבשמם**. אף crate לא תלוי בהם והם לא תלויים בדבר. היסטוריית הקומיטים חושפת "מפעל crates" אוטומטי (Devin/Copilot) בקצב של 18 ואז 72 crates לקומיט, Phase 1176→2615.

### 2.2 אפליקציית המוצר — gmin-spec / G.A.N.E NAV (TypeScript, 418 קבצים)

זו **המערכת הקרובה ביותר לחזון** — אפליקציה מלאה ועובדת:
- **Frontend:** React 19 + Vite + Tailwind 4, PWA עם Service Worker (4 מטמונים, LRU לאריחי מפה), 58 פאנלים בקבוצות NAV/INTEL/OPS/AI/SYS, i18n ל-40+ שפות עם עברית וערבית RTL, **מערכת פרופילי רכב מלאה ל-11 סוגי רכב** (הדרישה הקריטית מהמסמך — קיימת בקוד!).
- **49 מנועי צד-לקוח אמיתיים:** ESKF בן 15 מצבים (עם ציטוט מאמר Solà), PDR, multi-constellation, urban canyon, tunnel recovery, ניתוב OSM, סנכרון offline ב-IndexedDB ועוד.
- **Backend:** Express + tRPC (15 routers), מנועי traffic pipeline / crowd intelligence / geofence / ETL / moderation אמיתיים, Stripe מלא, Redis pub/sub עם fallback, OpenTelemetry + Prometheus + Sentry.
- **DB:** סכמת MySQL/Drizzle עם 33 טבלאות ו-9 מיגרציות (משתמשים, ציים, משימות C4ISR, טלמטריה עם שכבות retention, שיתוף פעולה, POI...).
- **שכבת החוזים (`shared/contracts/`, ~12,000 שורות)** — נכס יוצא דופן: 22 מודולים של דרישות הנדסיות ברות-אכיפה עם טסטים — 12 SLOs פורמליים (זמינות מיקום 99.9%, p99<100ms), מודל איומים STRIDE עם 6 אזורי אמון, 8 מכונות מצבים פורמליות, מטריצת כשלים, שרשרת ראיות קריפטוגרפית, מטריצות תאימות ISO 26262 / DO-178C / IEC 61508.
- **בדיקות:** 16 חבילות Vitest + 9 חבילות Playwright e2e + k6 load tests + Prometheus/Grafana.

### 2.3 gane-project v1.0.0 (Node + Capacitor) — המסלול לאנדרואיד

חבילה מאומתת (יומן אימות של Devin: **28/28 טסטים עברו**, שרת חי, התמדה לדיסק): backend משימות REST עם JSONL, מודולי בטיחות (safetyInvariants, mapMatcher, truthHierarchy), פרויקט Android Capacitor עם `GnssPlugin.java` ל-GNSS גולמי, זיהוי התנגשות דרך DeviceMotion, ומדריך אריזת APK/TWA מלא.

### 2.4 navigation2 — עותק לא-נגוע של ROS2 Nav2

Clone רדוד של upstream (v1.4.0, 50 קומיטים, אפס שינויים מקומיים, **אפס אזכורי aurora/gane בקוד**). כרגע ללא כל חיבור לפרויקט.

### 2.5 נכסים נוספים

- `Trade` (בתוך ZIP) — frontend נוסף בעיצוב הקוונטי, מבוסס Firebase, עם VehicleProfilePanel.
- `Gane---the-last-project` — ריפו ריק (README בלבד).
- ZIP בשם anthropics-skills שמכיל בפועל פרוטוטייפ אחר ("AEGIS/Brainiac") — שם קובץ מטעה.
- `VERIFICATION-LOG-2.txt` — **פגום לחלוטין** (כולו בתים אפסיים).

---

## 3. מה אין / מה חסר (פערים מסודרים לפי חומרה)

### קריטי
1. **אין חיבור בין ליבת ה-Rust למוצר ה-TS.** שתי מערכות מקבילות: ל-Rust יש REST משלו, ל-TS יש מנועים משלה ב-JavaScript. אין גשר (API/WASM/FFI). ההשקעה בליבת ה-Rust לא מגיעה למשתמש.
2. **אין מקור אמת יחיד.** ארבע ריפוזיטוריות זהות ביט-לביט + עותקי ZIP של הריפו בתוך עצמו (~19MB ארכיונים בגיט, כולל ZIP שמכיל ZIPים). כל תיקון עתידי יתפצל.
3. **אין נתונים אמיתיים בליבת ה-Rust.** אין קליטת NMEA/gpsd/חומרה, אין ייבוא OSM לגרף הכבישים (הגרף בזיכרון בלבד), אין התמדה (persistence).
4. **92% מהקוד הוא ניפוח** שמסתיר את הליבה האיכותית, מאט CI, ומטעה בספירות ("2,606 crates, 19,509 tests" — בפועל 2,452 ו-18,497, שמהם רק ~4,507 משמעותיים).

### גבוה
5. **9 אינטגרציות חיצוניות לא מוגדרות** באפליקציית המוצר: MySQL, פרוקסי המפות (Forge/Google), OAuth, Stripe, Redis, מפתחות Mapbox/HERE/TomTom, S3, Sentry, OTLP — בלעדיהן האפליקציה רצה במצב דמו בזיכרון.
6. **אין כתובת production** — ולכן ה-APK (P2 במסמך הדרישות) תקוע.
7. **תקלות P0 ממסמך הדרישות לא מאומתות כסגורות:** כפתורים ריקים, ספאם התראות ללא השתקה, עקביות צבעים. (פרופילי הרכב — הדרישה הקריטית — כבר קיימים בקוד אך דורשים אימות במכשיר אמיתי.)
8. **CI לא מכסה את הענפים הפעילים** (מופעל רק על main/master/devin), ושער הטסטים (≥870) מסופק על-ידי טסטים תבניתיים חסרי ערך.

### בינוני
9. Cargo.toml מכיל 300 שורות members כפולות (233 crates מוכרזים פעמיים); אין `[profile.release]` (LTO, strip).
10. README מצהיר "210+ crates / 4,600+ tests" — לא תואם את המצב בדיסק.
11. rate limiting ב-TS הוא in-memory (לא שורד ריבוי מופעים); מודרציה ו-crowd intelligence לא מתמידים לדיסק.
12. navigation2 — fork ללא ייעוד מוגדר.
13. מסמכי החזון באנגלית למרות שהמוצר עברית-תחילה; אין מסמך ארכיטקטורה מאוחד שמסביר איך ארבע היחידות אמורות להתחבר.

---

## 4. מה חייב (סדר פעולות מומלץ)

### שלב א' — קונסולידציה (שבוע)
1. להכריז על **nav-gan-1 כריפו הקנוני** ולארכב את Gane / Amjad-gane / Gane--by-DEVIN / Gane---the-last-project (או להשאירם כ-mirrors לקריאה בלבד).
2. להסיר את כל קבצי ה-ZIP מהגיט → GitHub Releases; למחוק את הלוג הפגום; לתקן את שורות ה-members הכפולות.
3. **להעביר את 2,254 ה-crates התבניתיים לענף ארכיון** (או תיקיית `generated/` מחוץ ל-workspace) ולהשאיר ~200 crates אמיתיים. תוצאה: CI מהיר פי כמה, ספירות אמת, קוד קריא.
4. לעדכן README למספרים אמיתיים ולתקן את שער הטסטים ב-CI למדידת כיסוי אמיתית (cargo-llvm-cov).

### שלב ב' — איחוד המוצר (2-4 שבועות)
5. **החלטת ארכיטקטורה:** אפליקציית G.A.N.E NAV (ה-TS) היא המוצר; ליבת ה-Rust היא מנוע המיקום/האמינות. לחבר: לקמפל את aurora-fusion/gnss/integrity/routing ל-**WASM** לשימוש בצד לקוח, או להציב את `aurora-nav` כשירות עם החוזה מ-`shared/contracts/apiEnvelope.ts`.
6. להגדיר את 9 האינטגרציות (DB, OAuth, Stripe, Redis, מפות, S3, Sentry, OTLP) ולפרוס ל-URL קבוע.
7. לסגור את ה-P0 מהדרישות: אימות כפתורים/תוויות, מנגנון השתקת התראות, עקביות ערכת נושא, בדיקת פרופילי רכב במכשיר.
8. לבנות APK עם Bubblewrap לפי המדריך הקיים (`com.gane.nav`) ולפרסם.

### שלב ג' — נתוני אמת ושדרוג (מתמשך)
9. ייבוא OSM לגרף הניתוב של Rust + מגבלות רכב (גובה/משקל/רוחב) בפונקציות העלות — זה מממש את "ניתוב לפי סוג רכב" (P1) בליבה החזקה.
10. קלט GNSS אמיתי (gpsd/NMEA/Android raw GNSS דרך GnssPlugin הקיים) אל תוך ה-EKF.
11. התמדה: SQLite/Postgres לטלמטריית Rust; Redis ל-rate limiting; דיסק ל-crowd intelligence.
12. להחליט על navigation2: לשלב רק אם יש יעד רובוטי/אוטונומי אמיתי — אחרת להסיר את ה-fork.
13. לתרגם את מסמכי הליבה לעברית ולכתוב מסמך ארכיטקטורה מאוחד אחד.

---

## 5. הצעות ייעול נוספות

- **CI:** מטריצת בנייה לפי crates שהשתנו בלבד; cargo-nextest; cache משופר. אחרי הסרת הניפוח — זמן CI יירד מסדרי גודל.
- **איכות Rust:** הוספת `[profile.release]` עם lto="thin", codegen-units=1, strip=true; clippy pedantic על הליבה.
- **תיעוד כנה:** badge-ים שמשקפים את המדדים האמיתיים; ה-4,507 הטסטים המשמעותיים + 88 קבצי הטסטים האדברסריאליים הם נכס שיווקי אמיתי — עדיף להציג אותם מאשר מספר מנופח.
- **ניהול דרישות:** להמיר את מסמכי ה-todo/דרישות המפוזרים ל-GitHub Issues עם תוויות P0/P1/P2, במקום "פאזות" של ייצור crates.
- **שכבת החוזים כשער CI:** ה-contracts ב-TS הם תשתית מצוינת — להריץ את טסטי הקונפורמיות שלהם כשער חובה בכל PR, ולהחיל את אותם SLOs גם על ה-API של Rust.

---

## 6. שורה תחתונה

יש כאן **ליבה הנדסית אמיתית ואיכותית** (EKF, PVT, RAIM, Dijkstra, API רץ ומאומת) ו**אפליקציית מוצר מרשימה** (PWA בעברית עם פרופילי רכב ושכבת חוזים הנדסית נדירה בהיקפה) — אבל הן **לא מחוברות**, קבורות תחת 92% קוד תבניתי מנופח, ומפוזרות על פני ארבעה עותקים זהים ושלל ארכיונים. העבודה הנדרשת אינה "לכתוב עוד" אלא **לאחד, לנקות, לחבר ולפרוס**.
